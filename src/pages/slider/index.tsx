import { Component, createRef, useEffect, useRef, useState } from 'react'

export default function () {
  return (
    <body>
      <div className='page slider js_show'>
        请直接使用小程序的 Slider 组件
      </div>
    </body>
  )
}
